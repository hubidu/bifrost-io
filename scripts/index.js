const getUserAgent = require('./get-user-agent')
const highlightElement = require('./highlight-element')
const dehighlightElement = require('./dehighlight-element')
const getViewportSize = require('./get-viewport-size')

module.exports = {
    getUserAgent,
    highlightElement,
    dehighlightElement,
    getViewportSize
}